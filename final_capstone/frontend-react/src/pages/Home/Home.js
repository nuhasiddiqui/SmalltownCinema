import Layout from "../../components/ui/Layout";
const Home = ({ children }) => {
    return (
        <Layout>
            <div>
            </div>
            {children}
        </Layout>
    );
}
export default Home;